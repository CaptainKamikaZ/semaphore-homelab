---
- hosts: all

  become: true
  tasks:
    - name: Update apt packages
      ansible.builtin.apt:
        update_cache: yes
        upgrade: dist

    - name: Remove unused packages
      ansible.builtin.apt:
        autoremove: yes
        purge: yes